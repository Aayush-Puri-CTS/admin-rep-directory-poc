// ⚠️ SERVICES_GROUP is a proposed value — confirm against the legacy
// PartyRelationshipType enum (or team convention) before running migrations.
export enum PartyRelationshipType {
  SERVICES_GROUP = 'SERVICES_GROUP',
}
